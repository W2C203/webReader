doctype html
html(lang='zh-CN')
    head
        title loginTest
        link(rel='stylesheet', href='/styles/joeTestStyle.css')
        script(src='/scripts/jquery-2.1.1.js')
    body
    h1 登录测试
    form(id='form1')
        div.form-row
            label
                span 用户:
                input(type='text',name='user_name')
        div.form-row
            label
                span 密码:
                input(type='password',name='password')
        div.form-row
            input(type='submit' value="登录",id='submit1')
    h1 拉取已购买书籍测试
    h1 （已登录的情况 不存在用户不存在 数据库中测试用户有3个 admin fyandmin feiyue）
    form(id='form2')
        div.form-row
            label
                span 用户:
                input(type='text',name='user_name')
        div.form-row
            input(type='submit' value="查询",id='submit2')

    script(src='/scripts/event.js')